name: Test plugin

on:
  workflow_call:
    inputs:
      irods_package_version:
        description: The package version of iRODS used to compile the plugin (e.g. 5.0.2-0~trixie).
        required: true
        type: string
      build_artifact_suffix:
        description: The name used to uniquely identify build output across workflow jobs (e.g. debian_13-5.0.2-0).
        required: true
        type: string
      plugin_package_directory:
        description: The directory name which is expected to hold the plugin packages (e.g. 'Debian gnu_linux_13'). Required by the irods_python_ci_utilities module.
        required: true
        type: string
      irods_testing_environment_project_os:
        description: The OS component of the project directory to test against. When joined with 'irods_testing_environment_project_db', it must match a directory under the projects directory within the iRODS Testing Environment repository (e.g. debian-13).
        required: true
        type: string
      irods_testing_environment_project_db:
        description: The DB component of the project directory to test against. When joined with 'irods_testing_environment_project_os', it must match a directory under the projects directory within the iRODS Testing Environment repository (e.g. postgres-16).
        required: true
        type: string

defaults:
  run:
    shell: bash

jobs:
  test_plugin:
    name: "Test: ${{ inputs.irods_package_version }}, ${{ inputs.irods_testing_environment_project_os }}, ${{ inputs.irods_testing_environment_project_db }}"

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Download build output
        uses: actions/download-artifact@v7
        with:
          name: build_output-${{ inputs.build_artifact_suffix }}
          path: ${{ inputs.plugin_package_directory }}

      - name: Set up docker compose
        uses: docker/setup-compose-action@v1

      - name: Set up iRODS testing environment
        run: |
          git clone -b main --depth 1 --single-branch https://github.com/irods/irods_testing_environment
          cd irods_testing_environment
          python3 -m venv venv_testing_env
          . venv_testing_env/bin/activate
          python --version
          pip install docker GitPython cryptography
          pip freeze

      - name: Run tests
        run: |
          ls -l "${{ inputs.plugin_package_directory }}"

          # This directory will hold the output from the test run.
          test_output_dir="$(pwd)/plugin_test_output"

          cd irods_testing_environment
          . venv_testing_env/bin/activate

          # Disable immediate exit on error. This is needed so that useful info in the XML test output
          # can be printed to the console and shown in the workflow output.
          set +e

          # Run the tests!
          project_dir="${{ inputs.irods_testing_environment_project_os }}/${{ inputs.irods_testing_environment_project_os }}-${{ inputs.irods_testing_environment_project_db }}"
          python run_plugin_tests.py \
            "${GITHUB_REPOSITORY#${GITHUB_REPOSITORY_OWNER}/}" \
            --project-directory "projects/${project_dir}" \
            --irods-package-version ${{ inputs.irods_package_version }} \
            --plugin-package-directory .. \
            --output-directory "$test_output_dir" \
            -v
          test_run_ec=$?

          # Print the summary line in each XML file. This line holds info like the number of passing,
          # failing, and skipped tests.
          echo '--- TESTSUITE SUMMARY (BEGIN) ---'
          find "$test_output_dir" -name '*.xml' -type f | xargs grep -h '<testsuite '
          echo '--- TESTSUITE SUMMARY (END) ---'

          # Print the failure info so that developers can determine what needs attention.
          if [ "$test_run_ec" -ne 0 ]; then
            echo '--- TEST FAILURES (BEGIN) ---'
            find "$test_output_dir" -name '*.xml' -type f | xargs grep -hA 3 '<failure type'
            echo '--- TEST FAILURES (END) ---'
          fi

          echo '--- SKIPPED TESTS (BEGIN) ---'
          find "$test_output_dir" -name '*.xml' -type f | xargs grep -hC 1 '<skipped type'
          echo '--- SKIPPED TESTS (END) ---'

          exit "$test_run_ec"
